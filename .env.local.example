# Live Ops Helpdesk — Environment Variables
# Copy this to .env.local for local development

# Socket.io server URL (client-side — must be NEXT_PUBLIC_ prefixed)
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
