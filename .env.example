# Optional: override the socket server URL (leave blank to use same-origin)
# On Railway/Render, set this to your deployed backend URL
# On Vercel with custom server, leave this blank
NEXT_PUBLIC_SOCKET_URL=

# Server port (default: 3000)
PORT=3000
